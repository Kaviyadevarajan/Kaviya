def is_prime(n: int) -> bool:
    return n > 1 and all(n % i for i in range(2, int(n**0.5) + 1))

print("29 is prime" if is_prime(29) else "29 is not prime")
print("407 is prime" if is_prime(407) else "407 is not prime")
